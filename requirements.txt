# MarkSix Probabilistic Forecasting System - Main Requirements
# For development dependencies, use: pip install -r requirements/dev.txt

# Include core dependencies
-r requirements/base.txt

# Essential testing dependencies for basic functionality
pytest>=7.0.0
pytest-cov>=4.0.0

# Environment management reminder
# To set up the full environment:
# 1. conda env create -f environment.yml
# 2. conda activate marksix_ai
# 3. pip install -r requirements/dev.txt  # For development

# Quick start for testing only:
# pip install pytest pytest-cov